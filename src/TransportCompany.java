public class TransportCompany {
    Transport createShip() {return new Ship();} // корабль

    Transport createTruck() {return new Truck();} // грузовик

    Transport createAirplane() {return new Airplane();} // самолёт
}
